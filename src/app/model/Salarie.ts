export class Salarie {
  id: string;
  firstName: string;
  lastName: string;
  country: string;

  constructor(id: string, firstName: string, lastName: string, country: string) {
    this.id = id;
    this.firstName = firstName;
    this.lastName = lastName;
    this.country = country;
  }
}
